
/**
 * @namespace tactic.builder
 */

/**
 * @typedef {Object} tactic.builder
 * @typedef {Object} tactic.builder.props
 * @typedef {Object} tactic.builder.layers
 * @typedef {Function} tactic.builder.layers.AbstractLayer
 * @typedef {Function} tactic.builder.layers.BannerLayer
 * @typedef {Function} tactic.builder.layers.JointLayer
 * @typedef {Function} tactic.builder.layers.SequenceLayer
 * @typedef {Function} tactic.builder.layers.FrameLayer
 * @typedef {Function} tactic.builder.layers.ImageLayer
 * @typedef {Function} tactic.builder.layers.TextLayer
 * @typedef {Function} tactic.builder.layers.VideoLayer
 * @typedef {Object} tactic.builder.objects
 */

/**
 * @typedef {Function} utils.Timeline
 * @typedef {Function} utils.arrayIndex
 * @typedef {Function} utils.getElementsByKey
 * @typedef {Function} utils.getElementWidth
 * @typedef {Function} utils.getElementHeight
 * @typedef {Function} utils.isTypeOf
 * @typedef {Function} utils.addEventSimple
 * @typedef {Function} utils.removeEventSimple
 * @typedef {Function} utils.getStyle
 * @typedef {Function} utils.getAssetSource
 * @typedef {Function} utils.getAssetUrl
 * @typedef {Function} utils.selectProperty
 * @typedef {Function} utils.watchFont
 * @typedef {Function} utils.hasClass
 * @typedef {Function} utils.addClass
 * @typedef {Function} utils.addClasses
 * @typedef {Function} utils.removeClass
 * @typedef {Function} utils.removeClasses
 * @typedef {Function} utils.getStyle
 * @typedef {Function} utils.createCssRule
 * @typedef {Function} utils.deleteCssRule
 * @typedef {Function} utils.createCssString
 * @typedef {Function} utils.createCssFontString
 * @typedef {Function} utils.mergeObjects
 * @typedef {Function} utils.extractExceptions
 * @typedef {Function} utils.wrapElement
 * @typedef {Function} utils.replaceMacros
 * @typedef {Function} utils.setAttribute
 * @typedef {Function} utils.removeAttribute
 * @typedef {Function} utils.CSVToArray
 * @typedef {Function} utils.GestureListener
 */